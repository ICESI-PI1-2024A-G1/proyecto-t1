from django.apps import AppConfig


class EmailcontactConfig(AppConfig):
    """App configuration for emailContact."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.emailContact"
